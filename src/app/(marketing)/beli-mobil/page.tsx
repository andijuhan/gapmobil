import SearchFilter from '@/components/SearchFilter';

const page = () => {
   return (
      <div className='bg-[#ebebeb] pt-[80px]'>
         <SearchFilter />
      </div>
   );
};

export default page;
